# Port for the express server to run on
PORT=5000

# PostgreSQL database connection string (e.g., from Neon Console)
DATABASE_URL=postgresql://username:password@ep-host-name.region.aws.neon.tech/dbname?sslmode=require
